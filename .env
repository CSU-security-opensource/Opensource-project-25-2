# 사용자 정의 DB 설정
DB_USER=jiun
DB_PASSWORD=dkflfkd135!
DB_NAME=open
DB_PORT=3306

# 🚨 여기에 위에서 확인한 Windows Host IP를 넣으세요!
# 예시: 172.20.128.1 (실제 환경에 따라 다름)
DB_HOST=172.18.22.48

# 최종 DATABASE_URL (WSL 환경용)
DATABASE_URL=mysql+pymysql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}